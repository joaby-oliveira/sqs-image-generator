# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build sqs-image-submitting`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t sqs-image-submitting`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system sqs-image-submitting && \
          adduser --system -G sqs-image-submitting sqs-image-submitting

COPY dist/apps/sqs-image-submitting sqs-image-submitting
RUN chown -R sqs-image-submitting:sqs-image-submitting .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix sqs-image-submitting --omit=dev -f install

CMD [ "node", "sqs-image-submitting" ]
